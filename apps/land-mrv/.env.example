# Land_MRV (ForestTrack) — configuración
#
# URL base del backend FastAPI de Land_Stratify (strat_router, Cloud Run).
# Si se deja vacía / sin definir, la app corre en MODO DEMO con un engine
# simulado client-side (mismo contrato de datos, sin GEE).
NEXT_PUBLIC_STRATIFY_API_URL=
